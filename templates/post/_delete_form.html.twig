<form method="post" action="{{ path('dashboard_post_delete', {'id': post.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir effacer ce post ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-outline-danger btn-sm mr-2"><i class="fas fa-trash text-danger pr-2"></i> Effacer</button>
</form>