{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary btn-sm mr-2"><i class="fas fa-save text-primary pr-2"></i> {{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}